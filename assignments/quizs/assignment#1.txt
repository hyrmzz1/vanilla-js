assignment - 1

Backend에서 사용가능한 programming language는 많지만, Frontend 에서 사용가능한 programming language는 오직 JavaScript 이다. (html css는 프로그래밍 언어 아님)
+) JS backend에서도 사용 가능!

JavaScript 는 모든 브라우저에 설치되어있다. => 따로 설치할 필요 X, JS file 실행하기 위해서 브라우저에 해당 file 열 필요 없음.

JavaScript 로는 비디오 게임도 만들 수 있다. => 3D stuff & 실시간 & 머신러닝 다 가능

JavaScript 는 단 10일만에 만들어진 언어로, 웹사이트에 interactivity를 추가하기 위해 만들어졌다.

HTML 파일에 JS 파일을 import하여 사용한다. => HTML 파일에서 CSS와 JS 코드 모두를 import함.
// CSS는 <head> 에서 <link rel = "stylesheet" href = "~~.css"> // 단축어는 link:css
// JS는 <body> 맨 아래에서 <script src = "~~.js></script>

브라우저의 콘솔창에서 JS code를 입력 및 확인할 수 있다. but 불편하므로 VSC에서 코드 작성 후 콘솔창에서 확인하기.